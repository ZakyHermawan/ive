# RUN: %ive %s -emit=ast 2>&1 | FileCheck %s

# CHECK:      ForExpr:
# CHECK:      VarDecl i<>
# CHECK:      BinOp: <
# CHECK:      Assign a

def main() {
  var a = 1;
  for i=0, i < 10, 1 {
    print(a);
    a = a + 1;
  }
}
